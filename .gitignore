node_modules

# S0: UI、游戏、实验、工具、独立入口和检查报告统一输出到 dist。
dist
# 历史产物保护：旧命令/旧分支生成的缓存仍不进入版本控制。
*-dist/
storybook-static

# static-preview/ 是 build:preview 的产物,但刻意提交以便直接分享静态预览,故不忽略。

.DS_Store
coverage
playwright-report
test-results
*.log
