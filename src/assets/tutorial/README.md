# 玩法手册局部图与截图来源

2026-09-13，内容11／`tide.guide.v1`。四张1600×900 JPEG保留为原图来源。新增首页「界面与操作」仅导入 `intents.jpg`：全图用于六区位置标注，按钮文字另用 SVG `viewBox="325 784 680 68"` 裁切放大底部操作栏，其他机制仍使用局部图解。它们来自正式构建的原战斗界面，用隔离浏览器与正式存档恢复入口生成，不是玩家当前存档。截图前收起提示，截图专用样式临时隐藏标记层，不修改玩法状态。

| 图片 | 权威步骤位置 |
| --- | --- |
| dice.jpg | T1.R1.kael.fix |
| intents.jpg | T2.R2.guard-bow |
| event.jpg | E1.attempt（已在UI中选中艾洛拉，尚未判定） |
| rewards.jpg | T3.R1.end |

现行手册实际使用：

| 局部图 | 原图区域（x, y, width, height） | 用途 |
| --- | --- | --- |
| event-detail.png | event: 866, 188, 145, 167 | 所选队员与成功面数 |
| hand-detail.png | rewards: 310, 650, 710, 132 | 两对、沉眠空面与金品质 |
| hand-preview.png | rewards: 640, 795, 132, 46 | 本轮两对加成 0.3 |
| factor-detail.png | rewards: 1128, 184, 181, 73 | 结算前的总倍率 2.97 |

「敌方意图与单条格挡」小节已移除；`intent-detail.png`（intents 原图区域：405, 135, 510, 445）仅保留为来源素材，手册不再导入或展示。

界面首页的操作教程另用同一 `intents.jpg` 裁切：`811 137 111 63` 放大敌人头顶生命与攻击读数；`300 430 282 348` 展示队员卡与下方骰子的一一对应。这些局部按 SVG viewBox 展示，不额外生成图片文件，也不改变战斗资产。

编号、引线、说明由 `GameHandbook.tsx` 绘制；骰面结构、六种战面与花色／品质图解直接复用真实 `ExpeditionFlatDieFrame`，力度 1～5 展示同组件右下角的刻痕局部，补给图标复用 `supplyArt`。空面使用 `blank-cross.svg` 大叉，命数沉眠仍单独显示；修改骰面图标后须刷新含空面的 `hand-detail.png` 等实战截图。不要把标注盖在要解释的数字或刻痕上。

命数／花色示例放大同一组件的左上角至 96×96 局部窗口，铭文品质使用 76×76 的右上角窗口，不再缩小整颗骰子来解释角标。手册花色局部沿用骰装页的深色花色底以增强轮廓对比，图形和命数仍来自真实组件；不修改战斗骰面的配色或规则。三行图文左侧为标题与核心规则，右侧为角标对照；图下仅保留名称或名称＋数值，不展示重复的局部图注，也不在正文重复枚举图中映射。

更新方式：先 `npm run build:game`，再 `node scripts/capture-tutorial-overview.mjs`；脚本同时生成原图与上述 PNG 局部图，更新后重新构建游戏。首次局部图从原 JPEG 等坐标裁切，未伪造战斗数据。脚本不连接真实模型，不读取或写入玩家浏览器的存档库。查阅与翻页不写玩法事实；战前总览位于 S3-1 播放后，“开始战斗”提交原剧情完成命令，不重复出征。

2026-09-13 内容归并后，七种补给图标直接放入「道具与补给」表格的道具行，不再另起一排重复标注；原 `supplyArt` 资产不变。两对、沉眠与金铭的实战局部图保留在「总倍率与金币结算」页，移除独立算例目录，没有删除图源。

事件页只将现有 `event-detail.png` 用作个人骰面适配信息的局部示意，配在参与者说明旁；移除编号、重复图例和单人成功率标题。该图不代表多人玩法界面，通用设计与当前实现的差异见主规格§6.3及手册审计记录。
