# UI 动效定向回退

日期：2026-09-16。用户要求：撤销改动，定向回退。状态：回退已执行；关键页面过渡已恢复，浏览器仍有标题关窗焦点失败，不能宣称全量验收通过。U3 批量迁移未通过体验验收，不再继续扩展。

## 回退边界

- 按本任务成功修改记录逆序还原，而非退回 Git HEAD。最新提交早于多项玩法／内容工作，不能整文件覆盖或重置仓库。
- 撤销 U3 A～F 普通控件、信息页、抽屉、地图／战斗操作层、页面交接的接入。恢复洋馆原房间入场、GameMenu 宽度过渡、出征队伍站位／尺寸过渡、遮罩淡变、战斗道具栏与账本移动、原加载揭幕和洋馆相位表现。
- 补回 U1 删除的 menu full／return 入场、对白打字、输入快进及对应测试。
- 保留当前整个 title 目录和独立 Logo：选中微展／扫光、双菱形错拍／浮动、加大后的背景视差、Logo 后四菜单逐项入场不变。
- 保留 U2 的 motion 依赖、Provider、UiContentTransition、三个基础按钮预设、首批调用和偏好设置；共享 RpgModal／UiModal 内核及同帧接焦行为按回退前版本保留，不扩展接入。它们不是全项目统一方案已经验收的证明。
- 玩法、存档 schema、交易／奖励规则、教程内容、美术素材、天气／3D 和专用演出不在此次回退范围。

## 可恢复性与核对

回退前备份：`/tmp/abyssa-motion-rollback.DTq0v8/before-rollback.tgz`，覆盖 src、docs、tests、config、scripts、包清单与 README。

第一轮精确还原 200 条成功修改记录、129 个文件，无匹配冲突；结果逐文件 SHA-256 校验一致。回退范围外 1,747 个源码文件在这一轮前后整体校验一致，包含整个 title／品牌目录。其后同步生成 CSS、修正测试和文档；浏览器发现旧 ribbon fallback 的优先级会覆盖标题按压规则，已将标题排除出该通用 fallback。共享模态的四个实现／测试文件按备份恢复为回退前版本，以保留标题窗口依赖。未修改标题本身或其他页面实现。

移除本轮新增的 UiSurface／UiCurtain、SceneInputBoundary／waitForScenePaint 及其迁移专用测试；移除断言“不得恢复旧动画”的约束和菜单“必须立即显示”的测试。共移除 17 个本次改造文件，均在备份中可恢复。原菜单 hook／CSS／测试三个文件已还原。UiModal 仅留作共享窗口内部实现，不恢复序幕和其他页面的批量接入。第一轮与最终文件校验清单见备份同目录 `rollback-manifest.json`、`rollback-final-manifest.json`。

## 验证

- app／tooling 类型检查、模块边界、产物与 motion token 一致性检查通过。
- 最终重新运行 17 组定向组件／页面测试，157 条断言全部通过，包括 title、menu、mansion、map、道具栏、账本、模态和页面交接。同帧接焦用例显式关闭默认关闭按钮，仅保留一个目标，避免把“先聚焦关闭按钮”的正确行为误报成失败。组件测试不替代下面仍有失败的真实浏览器焦点检查。
- 游戏构建通过：806 文件，135.36 MiB。
- menu → mansion → map → battle 的普通／系统减弱两种模式均已走通。普通模式的菜单不透明度、洋馆抽屉不透明度、常驻菜单宽度、出征队伍位置、道具栏与账本位移均采到至少 3 个不同中间状态。采样从真实 DOM 点击触发，避免把自动化等待点击的时间误当成动画时长。
- 一次普通模式样本：菜单 17 个、抽屉 6 个、菜单宽度 16 个、队伍位置 22 个、道具栏 24 个、账本 36 个不同中间状态。洋馆／地图仍有约 84／100ms 长帧，不宣称所有卡顿已经解决。报告：`dist/reports/motion-rollback-verified.json`（其中标题关窗还焦失败促使保留模态依赖，不能把此整份报告称为全绿）。
- 标题 Logo 后四项顺序入场与键盘跳过复验通过；四组普通／减弱 × 1600／1280 检查均走过选中菱形／视差与按压，随后在关窗还焦断言失败；触摸用例也在还焦失败。`dist/reports/motion-rollback-verified.json` 共 4 通过／5 失败，不能概括为“9 项通过”。
- 保留原共享模态内核后的复验 `dist/reports/motion-rollback-final.json`：入场 1 项通过，关窗还焦 1 项失败后主动早停，7 项未运行。直接浏览器诊断记录显示：打开窗口时来源按钮禁用／inert 后焦点落到 body，模态捕获了 body，关闭后尝试还给 body。U2 模态版本与回退前保留版本均复现；本次没有为追求全绿继续改标题／模态业务行为，也未删除相关断言。

## 限制

这是恢复既有表现的回退，不是全项目性能问题已解决的声明。老页面系统减弱、U2 手动偏好和旧过渡仍并存；不声称全局统一或稳定 60fps。后续是否重做动效，需在用户确认恢复效果之后另行决定，不自动恢复迁移任务。
