# M1：失效动效残留清理

日期：2026-09-17。对应[合并管理与瘦身计划](../plans/2026-09-17-ui-motion-consolidation.md)的 M1；不包含 M2 共享预设抽取或 M3 生命周期合并。

## 删除范围与调用链

4 个生产 CSS 文件仅做删除，没有更改任何当前生效的时间、曲线、位移、定位、美术或字号。

| 文件 | 删除内容 | 为什么失效／保留的实际实现 |
| --- | --- | --- |
| `mansion-dialogue.css` | `mansion-loading`、`mansion-room-card-in` 两个旧关键帧；`.mansion-loading span`、`.mansion-light` 两个孤立 reduced 选择器 | loading／light 无当前 DOM 消费者；房间抽屉已使用 `mansion-ui-motion.css` 的新动作 |
| `mansion-room-drawer.css` | 旧 300ms 动画声明与左右 ±16px 变量 | `mansion.css` 最后导入的新样式覆盖为 ±28px／560ms；保留 `transform:translateY(-50%)` 定位 |
| `sortie/sortie.css` | roster 的 drawer-in、quest 的 quest-in-l／r，共三个关键帧和三个调用 | 唯一生产消费者 MapPage 将它们放在 `MapPanel` 内；原内层动画被后加载 CSS 禁用 |
| `map-motion.css` | 专门禁用上述内层旧入场的 `animation:none` 补丁及说明 | 与旧定义一起删除，避免以后误删补丁复活双重入场 |

生产代码共删除 5 个旧关键帧及配套残留。没有增加运行时代码、依赖、DOM 层、RAF、监听或动画实例。

保留：`mansion-ready` 产出／升级标记、toast、ADV、真正的 `MansionTimeLoading`、世界天气、队伍集合 `abyssa-sortie-muster-in`、地图镜头，以及所有其他页面专属演出。Logo、标题菱形、menu 视差、角色切换、地图纸片弹簧、战斗和骰子完全未改。

入口核对：`SortieRosterPanel`／`SortieQuestPanel` 的生产调用只在 MapPage；route 最后加载 `map-motion.css`。`config/targets.mjs` 的 game 单页目标也使用完整 GameShell；`config/vite/plugins.mjs` 将旧 `map.html`／`mansion.html` 入口重定向到 `index.html#/…`。本机 HTTP 检查两者均为 302，浏览器另经 `mansion.html` 冷启动完成验收，没有独立裸组件入口被漏掉。

## 浏览器前后对照

使用本机 5190 开发入口、独立 QA 档、1280×720 视口。不推进时间、不结束远征、不调整队伍与携带物；只开关面板和查看未开放节点说明。

| 对象 | 清理前 → 清理后 | 终态屏幕坐标／尺寸（CSS px，前后相同） |
| --- | --- | --- |
| 洋馆大厅右抽屉 | `mansion-room-drawer-arrive` 560ms、delay 0；正文 340ms、delay 100ms，均保持 | x 886.400，y 208.637，w 374.400，h 433.925 |
| 洋馆正门左抽屉 | 同上；负向进入，原纵向居中不变 | x 19.200，y 209.438，w 374.400，h 432.325 |
| 出征编队 | 外层 MapPanel 动画；内层 `animation:none`、`transform:none` 保持 | x 207.631，y 407.550，w 864.732，h 224.400 |
| 旧庄园右委托 | 外层 MapPanel 动画；内层没有 CSS 入场 | x 785.403，y 130.790，w 280.160，h 494.360 |

对上述四项，保存的前后终态读数（动画名／时长、透明度、位移与 DOMRect）逐项 JSON 比较一致；洋馆终态 `opacity:1`、`translate:none`，出征外层终态 `opacity:1`、`transform:none`。

不是只看终态：普通 UI 操作后立即读取到右抽屉 `translate:8.4753px / opacity:.697311`，左抽屉 `translate:-23.7023px / opacity:.153488`；编队外层 y≈6.876px／opacity≈.714，右委托外层 x≈38.547px／opacity≈.035，随后归位。说明当前动画仍有中间状态，没有因为删旧 CSS 而消失。采样未锁定同一动画时刻，不将这些数字当作逐帧性能基准。

额外复验潮声溶洞左委托（可预览、不可出发）、进入编队后返回原左委托、委托关闭和洋馆详情关闭。左委托未在删除前记录独立几何基线，因此不冒称有该分支的前后浏览器数值对照；其 ±40px 方向和 780ms 时序另由组件测试固定。

减弱动态：自动测试覆盖 MapPanel 的左右委托／编队／行囊为零位移、零时长及全可见初态；CSS 测试读取实际洋馆样式，确认产品 reduced 与系统媒体查询守卫保留。本轮未修改系统或用户动态偏好，不声称做过 reduced 模式的浏览器实测。

## 自动验证

- 11 个 app 测试文件共 91 项最终通过：MapPanel、MapPage、MapLoadoutPanel、useMapIntro、map-landmark-intro、createMapScene、MansionPage、useMansionIntro、MansionTimeLoading、useMansionPresentation、style-entry-boundaries。
- 第一次与构建并行时，MansionPage 有 3 项超过默认 5 秒测试上限；构建结束后以 `--maxWorkers=1` 单独重跑该文件，14 项全部通过。未修改超时阈值或测试断言。
- 新增 `MapPanel.test.tsx` 固定普通／减弱模式参数及退出期 `inert`／`aria-hidden`；这是配置契约测试，不替代真实浏览器插值验收。
- `tests/build/ui-motion-residue.test.mjs` 新增 3 项，读取实际 CSS 检查旧定义与调用消失、当前动作／定位／减弱守卫保留；连同已有 route-styles 测试，共 4 项通过。
- `typecheck:app`、`boundaries:check`、`motion:check`、`build:game`、`git diff --check` 通过。
- 生产源码扫描不再找到上述五个旧动画名称；洋馆兼容入口冷启动后未记录新的控制台错误。

本批没有改共享 UI 包，因此未重复运行 `build:ui`。没有做帧率或压缩包体的前后基准，不宣称删除死代码解决了首次开窗长帧。当前动画库完整保留；统一预设与真实组件库展示属于下一批 M2。
