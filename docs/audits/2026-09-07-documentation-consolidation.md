# 当前机制总览与历史文档整理记录

日期：2026-09-07。范围：文档、文档引用及读取归档剧本的测试路径。游戏规则、UI、资产与玩家存档未在本轮修改。

## 1. 整理结果

过去的总规格同时混排设计、阶段日志和当前实现，开场规划也没有覆盖全游戏机制。本轮重新读取当前默认装配、规则、进度／经济、应用服务及页面接线，重写[机制与闭环总览](../GAME_SYSTEMS_AND_CONTENT_SPEC.md)，作为实际机制的统一入口。

总览覆盖：新档与六人36面、回合与骰子资格、牌型／品质／铭约、生命与临时状态、编队共鸣、初战／维护／回忆、事件、七种道具与四类携带限制、商店／资金／结算、成长／装备／记事、AVG与洋馆边界、存档恢复／升级／二周目、工程与LLM，以及内容质量缺口。

[当前状态](../DESIGN_DECISIONS_AND_CURRENT_STATUS.md)只维护有效定稿、完成度与排期；[文档索引](../README.md)统一指向当前文件。初章仍是下一项制作工作，具体分镜／钩子／教学关卡尚未定稿，不能因本轮整理变成“已实现”。

## 2. 历史归档与保留

- 46份已结束计划、旧规格、旧剧本、阶段审计与引擎历史移入 `docs/archive/`。
- 另保留3份合并前快照：旧总规格、旧状态页、历次战斗UI计划。
- 每份历史文件增加归档标识并修正引用；旧“当前／待实施”仅表示写作当时。
- 当前 `docs/` 主区保留11份文档：3份总入口／状态、3份未完成计划、3份有效专项设计、2份当前审计。
- 人设／世界源稿、有效美术定稿、素材及有用技术参考保留。旧成长剧本仍供已有一致性测试读取，不代表写作质量已经通过。

逐文件原路径与新位置见[迁移清单](../archive/README.md)。不删除仍需解释旧存档与历史证据的内容。

## 3. 同步纠正的模块说明

runtime／client、core／application、apps／content及玩法包README统一明确当前默认为规则4／内容3；删除“商店仍未开放、成长待D5接入、默认完整庄园v3”等失效现状。

Battle开发手册保留旧接口技术细节，但标明 `dispatchBattleCommand`、AtomicEffect和独立Battle schema4属于规则1兼容面；补上当前D5与应用事务入口。旧模块版本名不能与当前应用协议混用。

唯一非Markdown调整是 `src/content/presentation/growth-stories.test.ts` 的剧本文件路径，指向归档位置；正文、预期和游戏实现均保持原样。

## 4. 证据与验证

- 已实际加载并通过当前Catalog校验：`abyssa.demo / contentVersion:3 / rulesVersion:4`，摘要 `8b0267560ab2037ce862de5d3f2bbe30e2b252b5bbdffddb19a5d505ad0f9080`。
- 归档核对：46个目标文件和3份快照均存在，46个旧位置已清出；当前主区为11份文档。
- 链接核对：扫描仓库97份Markdown、1324个本地引用，文件路径与章节锚点均无失效项；同时修复了整理前已有的5处失效链接及1处旧章节引用。
- 改动范围：与本轮开始时的884份文本文件哈希核对，非Markdown只变更了上述测试路径，无额外文件删除；变更文档的表格／代码围栏及 `git diff --check` 均通过。
- 定向测试：`npm exec vitest -- run --project app src/content/presentation/growth-stories.test.ts`，1文件／1测试通过；归档后仍可读取并核对原成长文本。
- 当前难度结论沿用[同日质量审计](2026-09-07-demo-experience-quality.md)的96条指定策略／种子样本，未伪称重新试玩或重新完成全套E2E。

本轮验证只证明文档整理及引用的一致性，不为现有难度、剧作和缺图提供新的内容验收结论。

本地检查原始输出保存在 `dist/reports/docs-current/`（忽略目录）：Catalog导出、迁移映射、基线哈希、链接检查和改动范围检查。该目录不是长期设计入口；长期结论以本文和迁移清单为准。
