# GM-S4：联调执行记录〔技术已收口，内容未通过〕

> 文档整理说明（2026-09-25）：下文按原实施日期理解；已删除的阶段引用改为文字，不改写当时的结果。原引用文件保存在文档索引列出的仓库外备份。

日期：2026-09-25。依据：执行计划（历史稿已清理）、[最小职责定稿](../plans/2026-09-24-airp-gm-demo-scope.md)。**最新：无损池化已解除容量阻塞，原结算零调用落账，次日GM新增1次成功；终点revision382，累计348／500，技术整链重放通过。** 详见[存档修复与清档验收](2026-09-25-airp-save-pooling.md)。内容问题没有被存储修复解决。

**下文保留初次执行到revision373的历史记录。** 其中“未apply／次日未执行／347次”是修复前事实，不是最新状态；报告已续跑，当前formal-save是完成档。用户随后明确授权清旧档，覆盖初次执行“不修改玩家存档”的范围约束。

本批没有子智能体、视觉验收、录屏、玩家存档修改、提交、推送或部署。没有修改业务提示词、r8条目、卡书、模型配置文件或游戏数据上限。

## 1. 实际停在哪里

新隔离报告：[airp-cl-f-7zp2cw](../../dist/reports/airp-cl-f-7zp2cw/run.json)，content24／context19／reader6。首日日程明确是药箱固定夹具，不计真实日度GM；其后均使用真实模型、正式规则引擎和脚本测试者指令。

| 环节 | 实际结果 |
| --- | --- |
| 提出→态度回应→明确接单 | 三轮9次调用，GM为false→true→true，进入waiting-action，没有自动出征 |
| 副本GM | 同档全局输入、原卡书、已读全文；安排入场、第三层目标、侧门三个条件节点，没有另造事件／道具 |
| 副本行动 | 真实战斗探索到第三层，目标证据conditionMet=true；三个节点全部读完、选择并结算 |
| 撤回 | 测试者在第三层实际选择leave；引擎结果extracted，带回6件普通掉落；药箱待交付 |
| 交付结果 | 明确deliver后生成两轮结果；第一轮出现新的关怀问句，实际回应后第二轮收束。GM为false→true；未重开独立反馈／告别场 |
| 最终结算 | 第二次模型结果合法并已保存，job=ready；拟好感+1及两条带说话者记忆尚未apply，不是已经获得 |
| 下一日GM | **未执行**；最终结算尚未落账，不能绕过边界继续调度 |

事件程序状态是resolved／delivery confirmed，但不等于最终变量／记忆结算完成。已应用的结算回执只有副本3笔；最终事件好感仍未增加。终点为revision373。

完整脱敏请求为报告内`request-321.json`至`request-347.json`；原输出、失败用量、冻结帧和依据在[原存档](../../dist/reports/airp-cl-f-7zp2cw/formal-save.json)。27次游戏调用，累计**347／500**，上游报告用量**1,308,556 tokens**，无未知用量调用。没有付费刷稿、静默换模型或场景隐藏第四次调用。

## 2. 三个不同问题，不混称模型失败

### 2.1 报告缩进膨胀：已修

revision316的报告缩进JSON为8,935,426字节，紧凑JSON为6,346,245字节。重载时缩进占用了8 MiB入口预算，导致脚本停在交付前，尚未调用模型。

运行器现在保存紧凑JSON；三个报告检查入口先无损去掉历史报告的JSON空白，再使用原生产校验。字符串内部空白、原文、数组及字段都不变，真实内容大小／复杂度限制仍保留。详见[故障记录](../../dist/reports/airp-cl-f-7zp2cw/s4-harness-failure.json)、[回归](../../tests/build/airp-report-archive.test.mjs)。旧报告未被重写。

### 2.2 最终结算输出额度：重试成功

第346次调用：108,012输入token，16,384输出token全部为上游报告的推理用量，公开正文0字符，输出被截断。这不是安全过滤的证据。

仅对这个失败结算显式使用32,768输出额度并保留旧帧；第347次返回1,868字符合法JSON，17,100输出token，其中16,100为推理用量。正文／封装／GM均未重跑，本地配置未改。最终任务两份冻结输入相同，各284,832字节；此处重复保存也是后续容量治理要处理的真实对象，不能删除旧失败来造通过。

### 2.3 实际存档落账容量：**未修，阻塞收口**

脱敏占位恢复为原连接信息后，当前紧凑存档8,341,810字节，上限8,388,608字节，只余46,798字节。不是缩进问题。最终apply新增回执、记忆和交接事实时触发`DataValidationError: $: JSON size limit exceeded`。

存量分布：facts 3,149,883字节，Director 2,403,167字节，airpGame 2,664,195字节；包括冻结输入、原响应、已读交接和历史结算。没有用删除卡书／正文、删除旧失败、仅保留程序结算或抬上限来绕过。

[零调用复验](../../scripts/check-airp-gm-s4-blocked.mjs)在独立内存副本中重现apply失败，确认原记录未改变。生成的[证据](../../dist/reports/airp-cl-f-7zp2cw/s4-blocked-verification.json)明确标记blocked，不是完整验收报告。

```sh
node scripts/check-airp-gm-s4-blocked.mjs dist/reports/airp-cl-f-7zp2cw
```

恢复时应优先复用已保存的合法结算结果，不应再次付费调用。需要先另行明确保留全文及旧帧的存档去重／引用复用改造，不能把增加模型额度或换模型当成容量修复。

## 3. GM与记忆验收

- 五轮洋馆GM均有独立全局快照、全文资料和对应已读依据；每轮严格正文→封装→一次GM。没有将场景伪装为日度planning。
- 药箱提出、态度回应和接单没有再次陷入“补讲四项任务字段”的循环。
- 结果首轮新问句仍待回应，所以GM保留一轮；下一轮明确收束，没有强制complete。
- GM标出了正文擅加“按约定去后院掩埋”的任务指引冲突。程序没有因此创建清理任务，原文未被封装改写。
- 自然实测中GM均返回空更正；**不声称真实模型已经示范了摘要修复**。有限更正、读后生效、旧源不复活和新承诺另建的证据来自明确标为模拟的回归。
- 正常结算已保存“玩家认可路线”“实际取箱”“艾洛拉关于旧药的说法”等不同来源；最后两条事件记忆仍在prepared中，尚不能当成次日GM已继承。

## 4. 文本与封装单独审读

| 正文 | 中文字符／段 | 对白占比 |
| --- | --- | --- |
| 提出首轮 | 699／29 | 34.5% |
| 提出回应 | 215／6 | 38.6% |
| 接单 | 496／17 | 32.1% |
| 入场 | 774／24 | 29.7% |
| 取得药箱 | 1063／28 | 26.6% |
| 侧门 | 857／24 | 38.6% |
| 结果首轮 | 620／19 | 37.3% |
| 结果回应 | 294／9 | 24.1% |

字符含标点，不含选项；数值用于对照，不代替人工判断。正文文件均在上述报告内。

请求缘由和目标可以理解；取得药箱、撤回、交付的实际顺序一致。但**内容未验收通过**：

1. 100／120字软建议分别写成215／294字；安全叮嘱、帽子、圣杖、晚风和感谢重复，旁白仍过多。
2. 部分候选态度又写成办事流程／承诺；结果文案增加“后院掩埋”的既有约定，虽未转成程序任务，仍是误导性文案。
3. 五轮洋馆中文均canonical且restored=false，未发现封装另编剧情。药箱目标与侧门的副本文字除最外层对白引号外也一致。
4. 入场诺玛两句纯中文末尾带括号动作，旧确定性提取器把“嚼碎糖块／拍了拍腰包”误当中文译文，只提取动作；实际reader5封装反而保留了完整中文。差异见复验证据。这是**提取器歧义**，不是封装自由改稿。没有在本批直接改历史reader导致旧冻结帧重放变化；后续须以版本化读取修复并覆盖这类样例。

## 5. 本批改动与检查

改动仅限验收脚本、回归与文档：

- [记忆接线回归](../../src/game-application/airp-memory/integration.test.ts)：未读／半读归档后恢复；结果保存前失败／已写入后回执失败均只重试保存，单次更正，无新调用。
- [场景GM回归](../../src/game-application/airp-director/scene-gm.test.ts)：补context19三调用、跨阶段和失败仅重试GM。
- [运行器](../../scripts/verify-airp-cl-f.mjs)：保存完整脱敏请求，单轮未结束时留在原阶段，紧凑报告和失败记录。
- [全链检查器](../../scripts/check-airp-cl-f-report.mjs)：区分固定日程夹具／真实调用、有效记忆／原账、场景三调用、资料全文及封装对照。保留`--check-only`。
- [报告空白适配](../../scripts/lib/airp-report-archive.mjs)、上述专用阻塞复验；不修改生产数据预算。

检查结果：

- 17文件104项Vitest通过，再单独补跑context19的2项通过，共106项不同用例，单worker。
- 8文件36项AIRP构建测试通过；新增报告适配与测试的独立strict JS类型检查通过。
- core／application／app三层类型、模块边界1448文件、r8 34条逐字一致通过。
- 旧context16／18短链、旧context12完整报告只读重放通过。
- `build:game`通过，815文件／146.68 MiB；`check:output -- game`通过。大chunk警告仍在。
- 对构建全部815文件扫描本地真实Key／端点及编码形式，命中0；未上传。
- 本批6份文档269个本地链接、`git diff --check`通过；最终账本347／500，无残留调用锁。
- 额外尝试整库`typecheck:tooling`：**未通过，107项诊断来自11个本批未改文件**，包括既有baseline／expressions脚本、素材导入及旧build测试；新增报告文件0项。没有借本批重写这些文件。

## 6. 收口结论与下一步

S0～S3仍完成；S4已执行，但真实全链未通过。优先级是：

1. 先定最小的存档引用复用／去重方案，保留原卡书、已读全文、原输出、失败帧与回执，不扩大GM权限；同时明确结算输出额度变更不必要复制全文输入的问题。
2. 修复后从本报告的ready结算继续落账，再跑真正的下一日GM；原失败保留，不重开药箱开头刷结果。
3. 单列历史提取器的纯中文括号歧义；文案冗长／重复仍待内容验收，不在这次联调中推倒r8。

存档去重是新的明确工程议题，不是再调一轮提示词能解决的问题。当前不发布“GM-S4已完成”。
