# 存档去重／清档与GM-S4技术收口

> 文档整理说明（2026-09-25）：下文按原实施日期理解；已删除的阶段引用改为文字，不改写当时的结果。原引用文件保存在文档索引列出的仓库外备份。

日期：2026-09-25。依据：存档计划（历史稿已清理）、[GM-S4初次记录](2026-09-25-airp-gm-s4.md)。

**结论：最终结算与次日GM的技术闭环已通过；文本质量仍未验收通过。** 本轮新增1次游戏模型调用，累计348／500。未修改提示词、原卡书、r8、场景三调用链或模型默认配置；无子智能体、视觉验收、录屏、提交、推送或部署。

## 1. 修复内容

- 持久化使用版本化文本／结构池：完全相同的长字符串与JSON子树只存一份，通过引用还原。不是摘要、不清空历史、不丢失败帧。
- 运行时仍使用原始对象；还原连对象字段顺序、字符串空白、所有原文也保留，避免历史`JSON.stringify`提示词帧变化。复用的可变容器各自独立，修改一处不串改另一处。
- 接通D5边界、目录读取、正式IndexedDB写入、手动档案替换／删除、导出／恢复和隔离验收运行器；同一事务内保存快照与回执，重试不重复发奖。
- 普通JSON／模型输入校验仍是8 MiB；存档按实际池化表示执行同样8 MiB物理预算。解码前检查引用图，禁止前向／悬空／循环引用及纯引用别名链；独立展开预算64 MiB、30万节点、40层。**不是把全局容量改成64 MiB**；唯一内容超过8 MiB仍拒绝。

实现入口：[无损编解码](../../src/game-core/contracts/pooled-json.ts)、[D5归档](../../src/game-application/versions/d5-archive.ts)、[存储适配契约](../../src/game-application/save-codec.ts)、[IndexedDB](../../src/game-infrastructure/storage/indexeddb.ts)。

## 2. 原故障的零调用复验

[证据](../../dist/reports/airp-cl-f-7zp2cw/save-pooling-verification.json)：取原revision373、ready的合法结算，在独立fake-indexeddb数据库执行正式运行时apply；同样从原记录在MemoryDB执行并比较，两条路径结果完全相同。

| 项目 | 应用前 | 应用后 |
| --- | ---: | ---: |
| 原始record JSON字节 | 8,341,780 | 8,393,890 |
| 池化完整archive字节 | 3,384,593 | 3,389,700 |
| revision | 373 | 374 |
| 已落账结算／记忆 | 3／3 | 4／4 |
| 艾洛拉好感增量 | 尚未落账 | +1 |

应用后的原始展开记录确实超过旧入口上限，池化落盘仍明显低于8,388,608字节。不是误把缩进缩小当修复。

原事实前缀、全部场景、原输出、失败尝试及两份冻结输入保持不变。重复apply后head、回执、记忆、好感均不再增加；导出后在新数据库恢复相等。该零调用检查没有修改玩家档案或付费重跑模型。

## 3. 同一真实流程续跑

1. 从同一隔离报告继续应用已保存结果：0次模型调用，进入day-after。
2. 按真实时钟推进到第3日，调用一次`gpt-5.6-sol`日度GM。125,563输入＋808输出＝126,371 tokens。
3. GM读取9份原资料、6个有效记忆目标及实际变量；完成药箱任务没有被重新当作活动任务。接受1个既有事件的生活后续“旧药箱的搭扣 · 后续”，不是重新领取原药箱任务。该新后续尚未生成正文／内容审定。
4. 终点revision382；药箱resolved／delivery confirmed，4笔结算、4份记忆、好感+1、openThreads为空；全链离线重放通过。

[运行记录](../../dist/reports/airp-cl-f-7zp2cw/run.json)、[整链复验](../../dist/reports/airp-cl-f-7zp2cw/verification.json)、[完整脱敏请求348](../../dist/reports/airp-cl-f-7zp2cw/request-348.json)。本次完整报告共28次真实游戏调用，累计348／500，报告用量1,434,927 tokens。首日日程仍是明确标记的固定药箱夹具，不冒充真实日度GM；自然实测GM更正输出为空，不冒充已演示语义更正。

最终脱敏归档3,494,940字节；恢复真实连接信息后的正式archive为3,494,955字节，原始展开record为8,654,838字节，缩小约59.6%。[完成档恢复复验](../../dist/reports/airp-cl-f-7zp2cw/save-pooling-current.json)在新隔离IndexedDB／MemoryDB中通过。原失败仍保留在attempt／history及[初次阻塞证据](../../dist/reports/airp-cl-f-7zp2cw/s4-blocked-verification.json)。旧阻塞复现脚本仅对应修复前版本，不能作为新版本成功门禁；现在使用：

```sh
node scripts/check-airp-save-pooling.mjs dist/reports/airp-cl-f-7zp2cw
node scripts/check-airp-cl-f-report.mjs dist/reports/airp-cl-f-7zp2cw --check-only
```

池化检查支持ready时实际应用或applied时恢复／幂等核对，分别写不同证据文件，不覆盖原来从373实际应用的证据。

## 4. 用户授权的旧档清理

默认`abyssa-game-v1`升至数据库version3时，一次性清空旧`saves`、`receipts`、`archive`，不迁移旧进度。退休`abyssa-save-slots-v1`目录在首次槽位读写时清空且不再收养；最近档案、继续／待提交操作、阅读游标与出征配置提示按新代际清除一次。新存档在以后启动中保留。

保留独立`abyssa-ai-connection`中的API地址／Key、音量／显示等偏好、素材缓存、角色卡、世界书与预设；其它数据库及自定义测试库不会被默认库切换逻辑清空。旧游戏进度不能在游戏内撤销，只有用户先前自行导出的副本可另行恢复。

**真实浏览器执行边界：** 当前没有接入用户原本地预览的标签页，不能声称已检查其清理后数量。该站点加载新代码并首次打开数据库时执行；本地预览刷新后可新建档案。origin之间隔离，未部署公网，不声称清空公网浏览器数据。保留的`dist/reports`属于诊断证据，不是供游戏继续的玩家档。

清理入口：[数据库切换](../../src/game-infrastructure/storage/game-database.ts)、[提示清理](../../src/game-infrastructure/storage/save-generation.ts)。独立API加密配置的实际读写保留、v1／v2升级只清一次均有功能测试。

## 5. 验证与遗留

检查结果：

- 9文件72项不同Vitest用例通过，单worker：7文件47项；另4文件42项，其中17项重复。含新编解码／清档测试、S3未读／半读与失败恢复、context14～19场景重放、低模式驱动、D5原有完整通关／失败重试／结算原子性。
- 6项Node测试通过：清档范围4项、报告归档空白2项。
- 原revision373合法结果的实际apply及revision382完成档恢复：两个零调用证据均通过；IndexedDB／MemoryDB一致、导出恢复相等、重复apply不增量。
- 完整真实链离线检查通过，调用数28、累计348／500，无残留调用锁。
- core／application／app三层类型通过；边界1454文件通过；r8全部34条逐字一致。
- 最终`build:game`与产物检查通过：815文件／146.69 MiB。扫描全部产物，真实Key／端点及编码形式命中0；未上传。
- 8份同步文档本地链接检查通过；`git diff --check`通过。没有视觉验收。

仍然分开的尾巴：

- 文本冗长、重复安全叮嘱、擅加“后院掩埋”的既有约定；本轮没有改文风或剧情。
- 历史reader对纯中文句末括号动作的提取歧义仍待版本化修复；实际reader5封装保留了完整中文。
- 同档运行内仍保留全文与历史，池化不是无限容量承诺；后续长周期容量与结算输入成本需另行评估，不属于本次短Demo收口。
- 构建大chunk警告仍在；上批已报告整库tooling的107项既有诊断，本轮不把未做的整库修复计为通过。
