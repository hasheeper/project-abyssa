# AIRP 0.2.x 三段流与后处理验收

> 文档整理说明（2026-09-25）：下文按原实施日期理解；已删除的阶段引用改为文字，不改写当时的结果。原引用文件保存在文档索引列出的仓库外备份。

日期：2026-09-10。当前版本0.2.1；三段流、原生接口、恢复专项和两场真实闭环通过。0.2.0的读后更新失败被保留，未冒充完整通过。这里验收任务分工及运行链路，不等于全DEMO内容或正式文学质量验收。

目标：Claude纯文字大纲 → Gemini纯正文 → DeepSeek格式化／修复 → AVG阅读 → DeepSeek授权记忆更新。无需新增供应商配置。只在原生rp执行和存储，Abyssa不直接调用模型。

格式化修复冻结正文与大纲，提供上一轮格式化原文及程序错误；最多两次尝试。枚举、长度、字段、旁白表情和正文保真均校验。未读场景不写记忆；首条样本仍只授权summary，不新增硬玩法变量。

## 安装与版本

本地Application：`a1abd682-8fdc-4ccb-a293-f84163f909dd`。Package 0.2.1 hash：`a82757391c1580e2342c810a61354aeeb3c442886db67b68e065cfd5a6ef9a08`；当前Release：`51c92407-ce8a-4e88-917b-35299a722561`，hash：`93b4c3d08198ceff51de814628b5f89a086afa48d35bdecfd4223166ec352182`。三槽继续使用scy-a原绑定，没有修改供应商默认值。

- 大纲：`claude-fable-5 [perplexity]`，纯文字。
- 润色：`gemini-3.8-flash`，纯正文。
- 格式化／修复、读后Updater：`deepseek-v4-flash`，两个独立Role／Pipeline。

后端已重启加载新包，游戏已构建。入口仍为[游戏](http://127.0.0.1:5176)与[rp](http://127.0.0.1:5175)。已有存档／Session继续绑定原Release，不静默迁移；内容10新连接可选择当前Release。

## 0.2.0发现的问题

Release `1884d2b2-82fb-4394-ae0c-353147f48b5c`。玩家Session `4b0d3980-51b6-4498-8cce-2e27dfe773db`。

两场生成均通过，耗时44.973／51.624秒；第一场读后摘要10.564秒成功。第二场摘要两次把patch字段`path`写成`pointer`，原生解析以`OPERATION_SHAPE_INVALID`拒绝，确认20.397秒后incomplete。共9次真实Run：7完成，2失败；不能计作两场完整闭环。

0.2.1在rp通用State Updater上增加可选反馈Port：冻结可信正文、State及policy，把上次原始JSON与错误送回同一小模型做有限修复。错误字段不由程序偷偷转换；越权／领域语义拒绝仍不重试。修正提示词的准确字段说明，并要求摘要保留原文歧义。失败档案未删除或改写。

## 确定性验证

- rp分批145项通过：AIRP、通用Workflow与State回归。新增覆盖正文格式、枚举、旁白表情、原文改写拒绝、格式重试耗尽、格式化中断、记忆JSON与pointer错误修复、反馈原子入库／不可变、重启／重放；旧无反馈配置的行为保持。
- 跨仓库原生客户端验证15次模拟调用通过；玩家实际玩法闭环8次模拟调用通过。故障修复由模拟Provider确定性注入，不声称真实供应商已出现所有故障。
- Abyssa 123项专项通过；4项原生浏览器测试通过，覆盖响应丢失与双窗口、格式失败后手写降级、required Updater拒绝、Session创建恢复。原生writer／Updater重启后实际玩家存档恢复再次通过。
- rp server／AIRP与Abyssa类型检查通过，所改源码lint／格式检查通过。Abyssa模块边界与游戏构建通过。
- rp全量架构检查仍被原有7个未接线Logo文件及旧报告阻断；本次新增的目录集中度、文件行数问题已消除，显式更新新增可选共享契约的签名，没有放宽架构限制。

## 0.2.1真实证据

玩家Session `aff23118-9428-4d8a-ac70-60c732c4b4f2`；独立开发Session `e02cf513-0e9f-4737-be7b-207499197e7a`。4个committed／restorable楼层、8个completed模型Run，本轮没有实际重试；自动修复另有上述确定性故障测试。

| 场景 | 大纲 | 纯正文 | 格式化 | 生成端到端 | 读后确认端到端 |
| --- | ---: | ---: | ---: | ---: | ---: |
| 归来 | 22.719秒 | 4.472秒 | 17.697秒 | 45.401秒 | 14.378秒 |
| 后续 | 27.314秒 | 8.990秒 | 11.458秒 | 48.265秒 | 9.674秒 |

已逐项核对原生输入／输出：大纲、正文均为纯文字；格式化消费的原稿等于writer原始Result，最终逐行拼接与原稿一致（仅去掉声明的行首标签及空白）。两次摘要都被原生接纳；第一次实际summary及精确ID进入第二次大纲和文本的编译输入，不是靠“又提到药箱”猜测记忆成功。

原生成与确认请求实际重放均`replayed:true`，新增楼层、Run、attempt为0，资金不变。证据：原生摘要（历史附件当前不可访问：`../../dist/reports/airp-4/live-scy-a-51c92407/native-summary.json`）、完整输入／执行证据（历史附件当前不可访问：`../../dist/reports/airp-4/live-scy-a-51c92407/native-evidence.json`）、来源与重放检查（历史附件当前不可访问：`../../dist/reports/airp-4/live-scy-a-51c92407/verification.json`）。

两个真实冻结档经正常导入UI通过AVG展示检查：正文、角色表情、展开的创作记录均可读，1440×900面板不越界；截图已人工查看，无未捕获错误、无额外API POST。证据：浏览器记录（历史附件当前不可访问：`../../dist/reports/airp-4/live-scy-a-51c92407/browser-qa.json`）、归来画面（历史附件当前不可访问：`../../dist/reports/airp-4/live-scy-a-51c92407/return-avg.png`）、后续创作记录（历史附件当前不可访问：`../../dist/reports/airp-4/live-scy-a-51c92407/followup-creation-record.png`）。旧本地5176静态服务出现空响应，已原端口重启为现有构建预览服务，未更改游戏存档。

四次DeepSeek已知用量合计12,575 tokens；Claude／Gemini未返回usage，不能记作零费用。本轮另有0.2.0诊断的9次真实Run，因此此次修改共17次真实调用，不能只报最终8次。

仍需后续内容调优：原稿有从“侧门撤离”补成“走得急”的无依据推断，且搭扣动作描写偏多。后处理应保留并暴露原稿问题，不能偷偷润色来掩盖；这不影响本轮分工与接口验收，但不能称为正式精修文学稿。摘要只写连续性，硬变量、奖励和关系阶段未授权给模型。

历史0.1.x记录见旧验收（历史稿已清理）。
