# O3-T：退潮岩窟玩家流程接线

2026-09-09。范围：用O2-T规则与O3-U画布接通初章。原首晨正文、骰面、伤害规则、庄园版式和动作参数保留。

## 当前入口

O3-T以内容7／规则4发布教学流程；收口时并行AIRP工作已将默认目录前移到继承它的内容8，相关叙事功能不在本包验收范围。标题新建→原序幕→原首晨S1／S2→岩窟抵达→四场真实战斗→追回及返馆AVG→领取一次报酬→自由洋馆／商店／地图。未完成初章时，其他自由页面链接会恢复当前章节。

内容5／6首晨可从标题继续时保留游标和已选分支续接；原档另留。已进入冒险的旧档沿用兼容豁免，不伪造教学通关。活动教程保存run、attempt、随机游标、真实消耗和有效教学证明。

## 已接内容

- 七个剧情槽使用共用AVG JSON schema，位于 `src/content/presentation/scenes/tide-cave.json`。S3-4三选一提交真实分支；局部翻页仅作为阅读缓存。所有新文案明确标为O1-W占位，分支反应留待精修。
- 四场继续使用原战斗控制器与动画队列，最后一击演完才切AVG。沿用首晨全宽舞台、原对话框、原上下退场及两侧入场；抵达使用背景与左上角题签。
- 自动高亮读取真实ROLL、固定、行动和回合事件。后续留骰／防御／补给／牌型／铭约只在满足条件时提示；可关闭，偏好持久保存。可选操作不锁通关。重试按run与attempt重新载入说明已读缓存，避免沿用上一尝试的局部状态。
- 本场重试和从入口重来复用原战间区域与操作按钮。恢复对应HP、补给、临时锈、金币及RNG；不发失败奖励、不推进时间。
- 返馆完成后结算真实战利品及一次8G报酬、归还配给、推进一个时段。不会发放庄园接管、成长、装备或玛亲征资格。
- 五张SVG轮廓为内部美术占位，洞穴复用现有背景；返馆复用首晨洋馆背景。通用勇者边框不带庄园红线，加载沿用统一清单与解码缓存。

## 验证

- 原庄园战斗、战间流程、AVG与首晨组件：4文件36项通过。
- 新增表现／提示模型：2项通过，覆盖合法骰面与目标、UNDO、关闭后恢复、剧情槽和占位素材。
- O2核心教程4项与应用教程3项通过；目录发布后，旧档续接／首晨／序幕11项回归通过。候选注入测试已改为使用已发布目录，避免重复注册内容7。另用既有真实游玩检查点复验了新周目及旧v3升级1项，核对当前默认目录与原档保留。
- 浏览器从真实首晨末节点存档进入，开启完整动画，使用原控件打完四场，选择C分支，刷新返馆AVG并领奖，再进入商店。四场主流程复验通过；没有在前端灌入骰面、HP或奖励。初次实施验收使用内容7；本次收口复查已将浏览器夹具改为当前默认包，内容7继续由规则和表现模型回归覆盖。
- 三个浏览器场景已分别通过：四战及一次领奖、URL门禁及团灭刷新／两种重试、标题新建→序幕→原首晨。重试验证逐项比较队伍HP、配给、RNG和时间；最新重试复验使用独立输出目录，避开并行测试删除录像临时文件造成的报告收尾冲突。
- 1280×720与1920×1080截图检查了高亮与原布局；另外审看了三选一、关底、返馆与领奖画面。修正占位敌图百分比高度导致不可见，以及AVG误带外框的问题。
- 全量类型检查、游戏构建、模块边界与无界面core/application导入检查通过；导入检查实际结算5次远征。`git diff --check`通过。

复验入口：`tests/smoke/tide-cave.spec.ts`。截图输出在忽略目录 `dist/reports/browser/tide-cave-*`；并行测试时使用独立端口和输出目录，避免清理彼此的临时报告：

```sh
ABYSSA_SMOKE_PORT=5208 npm exec playwright -- test -c config/playwright.config.ts --project=game --workers=1 --output=dist/reports/tide-cave/o3-browser tide-cave.spec.ts
```

本次独立重试报告在 `dist/reports/tide-cave/o3-retry-browser/`。

## 收口复查（2026-09-09）

检查范围为O3-T，保留并行AIRP改动。没有调整战斗版式、动作幅度、原首晨正文和冻结规则数据。

- 修正主角旧固定姓名漏出：补给目标和敌方意图在表现层按玩家身份显示；战斗记录先保留 `{{user}}`，渲染时使用同一姓名解析。原 `kael` ID和历史内容摘要不变。
- 修正手动／自动指引的关闭冲突：在自动教程上打开手动指引后，点击一次关闭会同时保存关闭偏好，不再重新露出底下的自动提示。
- 补齐当前默认包的验证：浏览器四战和恢复夹具显式使用当前默认目录（内容8），测试策略按该存档的真实内容摘要选择规则。内容7的独立兼容测试保留。
- 重新运行核心教程、应用事务／恢复、旧档续接、庄园控件、AVG与教程画布：8文件24项通过。新增断言验证真实攻击记录中的玩家占位符、道具目标姓名及冻结角色名称保留。首次并行运行遇到Vitest报告RPC超时，单worker完整复验无未处理错误。
- 全量类型、模块边界、无界面导入及独立静态游戏构建通过；使用独立构建、端口和报告目录，避免与并行测试互相覆盖。
- 当前默认内容8的三条浏览器流程全部通过（4.7分钟）：完整动画下的四场战斗／C分支／返馆刷新／一次领奖及商店；未完成章节的URL门禁／团灭刷新／本场与整章重试；标题新建／序幕／原首晨。新增检查验证自动教程上打开手动帮助后，一次关闭即生效，刷新仍保持关闭。
- 内容8的未完成章节备份使用现有“备份恢复”入口保留原存档身份；测试不再将其按普通导入复制为新档。没有放宽生产恢复规则。两种重试逐项核对HP、补给、RNG和时间；领奖后刷新不重复发奖，未误解锁庄园接管。
- 复看1280×720与1920×1080教学截图，以及三选一、关底、返馆、领奖和团灭画面。保持原版式与AVG组件，未见教程挤压布局或文字溢出。报告：`dist/reports/tide-cave/o3-closeout-smoke.json`；截图：`dist/reports/tide-cave/o3-closeout-browser/`（均为本地忽略产物）。

结论：O3-T工程接线收口检查通过。以下内容制作与真人体验验收继续独立保留，不以自动测试代替。

## 尚未验收

本包完成的是可试玩接线。正式敌图、正式对白及A／B／C各自反应、S2-6行军外景、音频仍待制作。真人新玩家教学时长、攻防选择与资源压力归O4-T；自动规则测试通过不代表数值与文案合格。原庄园压力不足的问题继续保留。
