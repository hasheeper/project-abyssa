# 2026-09-18 集成基线收口

状态：提交前收口完成，未发现阻断此次提交的问题。用户确认将当前相互依赖的工作区改动作为新基线，而不是只拆出最近几处战斗文件。

## 收录范围

- Motion for React、统一 CSS 控件／主板／窗口预设、组件库展示和动效检查工具；标题 Logo、视差、骰子和战斗等专用演出继续独立维护。
- menu、角色、出征、商店、洋馆等已完成的界面与动效调整，以及仓库／日志／整备共享窗口。
- 更新后的敌方舞台、布局测量与补位优化、局部悬浮更新、统一装饰暂停策略，以及旧庄园换层资源交接修复。细节见[战斗计划与验证](../plans/2026-09-18-battle-motion-performance.md)。
- 同一工作区中已接线的第一章／逐步教程、版本化内容与规则、AIRP 客户端／契约、独立 `llm` 工具及其测试、文档、静态美术，避免形成缺少依赖的提交。

这是一份可追溯的工程基线，不代表所有内容／美术定稿或体验问题已经解决；已回退的 U3 方案不恢复，Logo 动画不删除。

## 提交卫生与本轮修补

- 新增根目录 `.env*`、Python 字节码／`__pycache__` 忽略规则；既有 `llm/.gitignore` 保留本机配置、密钥、草稿与历史。示例密钥文件的四个 key 和 server token 均为空。
- `dist`、测试报告、依赖目录、真实存档和本机凭据不进入提交。候选文件未发现常见密钥模式或符号链接；最大新增素材约 7.81 MiB，无超限单文件。模式扫描不能替代完整安全审计。
- UI 包检查发现 CSS 将同目录图片错误写成 `../status-panel-emblem.png`／`../battle-frame-top.png`。显式固定 UI 资源输出命名，使 Vite 按实际的包根目录计算 URL；不修改美术。发布脚本和构建测试增加 UI 资源完整性校验，不能只检查图片文件存在。
- 更新旧庄园用例：通过真实菜单注册和 `GameMenu` 验证“扎营”仍置灰且提示尚未开放，不再寻找已移走的战场内旧按钮。教学文案断言跟随当前“成功判定面”、单条意图格挡和每轮行动次数的表述。不删用例、不放宽超时、不改玩法规则。
- 场景匹配测试在真实命令重放之间让出事件循环，避免纯微任务链占用 worker 太久而阻塞测试进度回传；仅改测试循环，不修改游戏实现或延长测试／RPC 超时。
- 清除一份剧本参考文档末尾多余空行。

## 验证

最终验收使用项目固定的 Node 22.23.2／npm 10.9.8，通过临时运行时执行，不修改系统默认 Node。最初 Node 26 的全量测试运行已主动停止，不作为最终通过证据。

| 检查 | 结果 |
| --- | --- |
| core／application／app／tooling 类型检查 | 全部通过 |
| 入口、模块边界、动效参数同步、纯核心导入 | 通过：19 个入口，972 个源文件／121 个 core 生产文件，124 个兼容导出，5 次无界面出征结算；动效参数同步通过 |
| 全量 Vitest 与修正复验（core／application／app） | 254 文件；合并通过证据覆盖 1917 个不同用例，5 个夹具条件用例未启用；不是一次全绿运行，详见下文 |
| 构建／发布回归 | 85／85 通过，包含新增 UI CSS 资源闭包检查 |
| UI 正式构建与包发布检查 | 通过；218 个构建产物，220 个包内文件，7 张外置 PNG |
| 游戏正式构建与资源完整性检查 | 通过；814 个产物；game／ui 资源完整性检查均通过 |
| 独立 LLM 工具测试／类型检查 | 40／40 通过，类型检查通过；仅本地 mock，不请求真实模型 |
| AIRP 调用预算脚本 | 8／8 通过 |
| 生产依赖安全通告检查 | `npm audit --omit=dev`：0 项通告 |
| 暂存区空白、缓存与敏感文件检查 | 通过；无禁止路径、符号链接或常见密钥模式匹配 |

### 全量测试与复验明细

- 首轮使用固定运行时、默认双 worker，耗时约 20 分 28 秒：253 文件通过、1 文件失败；1916 项通过、1 项失败、5 项跳过。唯一断言失败来自运行开始后才修正的 `GameHandbook` 旧格挡文案；该文件随后独立完整复跑 20／20 通过。
- 首轮另报告一次 `[vitest-worker]: Timeout calling "onTaskUpdate"`。检查发现 `tide-scene` 连续命令重放耗时 68.4 秒，其循环没有让出事件循环；Vitest 的 RPC 默认时限为 60 秒。与既有长流程测试一致，在此测试的每条命令之间加入零延时让出，不修改产品代码、不延长时限。
- 修正后，`tide-scene`、`guided-tide-model`、`GameHandbook` 在原双 worker 配置下完整复验：3 文件、33／33 通过，耗时 66.35 秒，无未处理错误；应用类型检查再次通过。这是定向复验，不将首轮报告改写成一次完整绿灯。
- 5 个未启用用例均在 `airp-pool.test.ts`，由 `ABYSSA_AIRP_FIXTURE`／`ABYSSA_AIRP_POOL_FIXTURE` 控制，覆盖旧 content8 结案升级及额外返程／战败／通关变体；本轮未提供这些环境变量。普通 AIRP 实际命令、归档、三条出征链与本地在线接线测试已运行通过，不把条件跳过计作通过。
- 原始 JSON 在被忽略的 `dist/reports/baseline-2026-09-18-{vitest,handbook,worker-recheck}.json`，本记录保留结果摘要，不将临时报告或其测试存档加入仓库。

## 保留的边界

- 本轮不调用收费模型，不部署／发布在线服务，不更改玩家存档。在线隔离与格式逻辑由本地测试覆盖，不把 mock 测试当作真实模型质量验收。
- 不宣称所有设备无卡顿；整备首次开窗长帧等既有体验问题仍保留，战斗也未做同条件修改前后 FPS／GPU 对照。
- 本轮未重新跑全部浏览器端到端用例；此前的逐页视觉验收与战斗夹具证据保留在对应专项文档。当前基线的自动测试与构建结论分开记录。

## Git 交付

本记录随基线一并提交；交付目标为当前 `main` → `origin/main`，普通提交与推送，不强制推送、不改写旧历史。实际提交标识以 Git 历史和交付回执为准。
